// Re-export shared types for frontend use
export type { Severity, Status, Role, Incident, AuditLog, Comment, PaginatedResponse } from '../../shared/types';
export type { User } from '../store/authStore';
